#include<stdio.h>
#include<string.h>
int main()
{
	char a[50];
	printf("input : ");
	gets(a);
	printf("\n");
	
	int i,j;
	int n=strlen(a);

	for(i=0; i<n; i++)
	{
		for(j=i; j<n; j++)
		{
			printf("%c",a[j-i]);
		}
		printf("\n");
	}
	return 0;
}
